# 第6章 虚拟存储器

## 6.1 虚拟存储器概述
### 6.1.1 常规存储器管理方式的特征和局限性
**常规存储器管理方式**的特征：
- 一次性：程序必须全部装入内存才能运行
- 驻留性：程序一旦装入内存，就会一直驻留到运行结束

**局限性**：
- 内存容量限制：程序大小不能超过内存容量
- 内存利用率低：程序的某些部分可能很少使用
- 多道程序度受限：内存容量限制了并发运行的程序数量

### 6.1.2 虚拟存储器的定义与特征
**虚拟存储器**是指操作系统为每个进程提供的一个虚拟的地址空间，这个地址空间的大小可以远大于实际的内存容量。

**特征**：
- **多次性**：程序可以分多次装入内存
- **对换性**：程序可以在内存和外存之间对换
- **虚拟性**：提供比实际内存更大的地址空间

**虚拟存储器的容量**：由计算机的地址位数决定，如32位系统的虚拟地址空间为4GB。

### 6.1.3 虚拟存储器的实现方法
**实现方法**：
- **请求分页**：在分页系统的基础上，增加请求调页和页面置换功能
- **请求分段**：在分段系统的基础上，增加请求调段和段置换功能
- **请求段页式**：在段页式系统的基础上，增加请求调页和页面置换功能

## 6.2 请求分页存储管理方式
### 6.2.1 请求分页中的硬件支持
**请求分页**是虚拟存储器的一种实现方式，需要以下硬件支持：

- **页表机制**：增加状态位、访问位、修改位等
- **缺页中断机构**：当访问的页面不在内存时，产生缺页中断
- **地址变换机构**：支持逻辑地址到物理地址的转换

### 6.2.2 请求分页中的内存分配与回收
**内存分配策略**：
- **固定分配局部置换**：为每个进程分配固定数量的页框，当发生缺页时，在进程自己的页框中选择页面置换
- **可变分配全局置换**：根据进程的需要动态分配页框，当发生缺页时，在全局范围内选择页面置换
- **可变分配局部置换**：根据进程的需要动态分配页框，当发生缺页时，在进程自己的页框中选择页面置换

**回收策略**：
- 当进程结束时，回收其占用的页框
- 当系统内存紧张时，通过页面置换回收页框

## 6.3 页面置换算法
### 6.3.1 最佳页面置换算法和先进先出页面置换算法
**最佳页面置换算法（OPT）**：选择未来最长时间不会使用的页面置换。

**优点**：缺页次数最少
**缺点**：需要预测未来的页面访问情况，无法实现

**先进先出页面置换算法（FIFO）**：选择最早进入内存的页面置换。

**优点**：实现简单
**缺点**：可能产生Belady现象（增加页框数反而增加缺页次数）

### 6.3.2 最近最久未使用页面置换算法和最少使用页面置换算法
**最近最久未使用页面置换算法（LRU）**：选择最近最久未使用的页面置换。

**优点**：性能接近最佳算法
**缺点**：需要记录页面的访问时间，实现复杂

**最少使用页面置换算法（LFU）**：选择使用次数最少的页面置换。

**优点**：考虑了页面的使用频率
**缺点**：需要记录页面的使用次数，实现复杂

### 6.3.3 Clock页面置换算法
**Clock页面置换算法**：使用一个环形链表管理页面，通过访问位来选择页面置换。

**基本思想**：
1. 初始化时，所有页面的访问位为0
2. 当需要置换页面时，从当前位置开始扫描
3. 如果页面的访问位为0，选择该页面置换
4. 如果页面的访问位为1，将其置为0，继续扫描

**优点**：实现简单，性能接近LRU

### 6.3.4 页面缓冲算法
**页面缓冲算法**：将被置换的页面放入缓冲池，分为空闲页缓冲池和修改页缓冲池。

**优点**：
- 减少磁盘I/O次数
- 提高页面置换的效率

### 6.3.5 请求分页系统的内存有效访问时间
**内存有效访问时间**：
- 访问内存的时间：m
- 缺页中断处理时间：t
- 缺页率：f

有效访问时间 = (1 - f) × m + f × t

## 6.4 "抖动"与工作集
### 6.4.1 多道程序度与"抖动"
**抖动**是指系统中频繁发生缺页中断的现象。

**原因**：多道程序度过高，导致每个进程分配的页框数不足，频繁发生页面置换。

**影响**：系统吞吐量下降，响应时间变长。

### 6.4.2 工作集
**工作集**是指进程在某段时间内频繁访问的页面集合。

**工作集模型**：
- 工作集大小：进程在时间t内访问的不同页面数
- 工作集窗口：时间t的大小

**目的**：根据工作集大小为进程分配页框，避免抖动。

### 6.4.3 "抖动"的预防方法
**预防抖动的方法**：
- **调整多道程序度**：根据系统内存容量调整并发进程数
- **工作集模型**：为每个进程分配足够的页框，使其工作集能够全部装入内存
- **L=S准则**：系统的缺页率等于页面置换率
- **挂起进程**：当系统内存紧张时，挂起部分进程

## 6.5 内存映射文件
**内存映射文件**是指将文件映射到进程的虚拟地址空间，通过内存操作来访问文件。

**优点**：
- 简化文件操作
- 提高文件访问速度
- 支持共享内存

**实现**：
- 使用系统调用将文件映射到虚拟地址空间
- 通过指针访问映射的内存区域
- 完成后解除映射

## 6.6 虚拟存储器实现实例
### 6.6.1 实例1：在Windows XP系统中实现虚拟存储器
**Windows XP的虚拟存储器**：
- 虚拟地址空间：4GB（32位系统）
- 页面大小：4KB
- 实现方式：请求分页
- 页面置换算法：改进的Clock算法
- 交换文件：pagefile.sys

### 6.6.2 实例2：在Linux系统中实现虚拟存储器
**Linux的虚拟存储器**：
- 虚拟地址空间：4GB（32位系统）或更大（64位系统）
- 页面大小：4KB（可配置）
- 实现方式：请求分页
- 页面置换算法：改进的Clock算法
- 交换分区：专门的交换分区或交换文件

### 6.6.3 实例3：在HarmonyOS中实现虚拟存储器
**HarmonyOS的虚拟存储器**：
- 支持32位和64位地址空间
- 页面大小：4KB
- 实现方式：请求分页
- 页面置换算法：自适应算法
- 内存管理：考虑移动设备的特点，优化内存使用

## 6.7 本章小结
- 虚拟存储器的概念和特征
- 请求分页存储管理方式
- 各种页面置换算法的特点和实现
- 抖动的原因和预防方法
- 工作集模型
- 内存映射文件
- 实际操作系统的虚拟存储器实现

## 习题6（含考研真题）